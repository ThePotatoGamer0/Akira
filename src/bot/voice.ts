export const waitingMessage =
  "Akira is still getting everything ready — hang tight! This won't be long.";

export function stub(feature: string): string {
  return `Akira's ${feature} isn't wired up yet, but she's on it. Thanks for being patient!`;
}
